# Meme CLI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 밈 미디어(영상클립/GIF/이미지)를 저장·검색·관리하는 CLI 도구를 만들어, AI 영상 제작 시스템이 적절한 밈을 자동으로 찾아 쓸 수 있게 한다.

**Architecture:** SQLite로 메타데이터를 관리하고, 미디어 파일은 `~/.meme-cli/media/`에 복사해 중앙 관리한다. 미디어 추가 시 `claude --print` 서브프로세스로 이미지를 분석해 태그·감정·설명·사용 맥락 메타데이터를 자동 생성한다. `--json` 플래그로 AI 친화적 구조화 출력을 제공해 외부 AI 에이전트가 검색 결과를 바로 활용할 수 있다.

**Tech Stack:** Python 3.11+, uv, typer, rich, sqlite3 (stdlib) — anthropic SDK 불필요 (Claude Code CLI 재사용)

---

## 파일 구조

```
meme-cli/
├── src/
│   └── meme_cli/
│       ├── __init__.py
│       ├── cli.py          # Typer CLI entry point (add/search/edit/list/get/remove)
│       ├── models.py       # MediaItem dataclass
│       ├── store.py        # SQLite CRUD layer
│       ├── ai.py           # Claude Vision 메타데이터 생성
│       └── search.py       # SQLite FTS5 기반 전문 검색
├── tests/
│   ├── conftest.py         # 임시 DB/미디어 디렉토리 fixtures
│   ├── test_models.py
│   ├── test_store.py
│   ├── test_search.py
│   └── test_cli.py
├── pyproject.toml
└── README.md
```

---

### Task 1: 프로젝트 스캐폴딩

**Files:**
- Create: `pyproject.toml`
- Create: `src/meme_cli/__init__.py`
- Create: `src/meme_cli/cli.py` (skeleton)
- Create: `tests/conftest.py`

- [ ] **Step 1: pyproject.toml 작성**

```toml
[project]
name = "meme-cli"
version = "0.1.0"
description = "밈 미디어 관리 및 검색 CLI"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
    "rich>=13",
]

[project.scripts]
meme = "meme_cli.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/meme_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
```

- [ ] **Step 2: uv로 패키지 설치**

```bash
uv sync
```

Expected: 의존성 설치 완료, `.venv/` 생성됨

- [ ] **Step 3: CLI skeleton 작성**

`src/meme_cli/__init__.py`:
```python
```

`src/meme_cli/cli.py`:
```python
import typer
from rich.console import Console

app = typer.Typer(help="밈 미디어 관리 CLI")
console = Console()


@app.callback()
def main():
    """밈 미디어 저장소 CLI"""
    pass


if __name__ == "__main__":
    app()
```

- [ ] **Step 4: CLI 진입점 동작 확인**

```bash
uv run meme --help
```

Expected:
```
Usage: meme [OPTIONS] COMMAND [ARGS]...
  밈 미디어 저장소 CLI
```

- [ ] **Step 5: tests/conftest.py 작성**

```python
import pytest
import tempfile
from pathlib import Path


@pytest.fixture
def tmp_store_dir(tmp_path: Path):
    """격리된 임시 저장소 디렉토리 반환"""
    media_dir = tmp_path / "media"
    media_dir.mkdir()
    db_path = tmp_path / "memes.db"
    return {"base": tmp_path, "media": media_dir, "db": db_path}
```

- [ ] **Step 6: 커밋**

```bash
git init
git add pyproject.toml src/ tests/
git commit -m "chore: project scaffold with typer CLI skeleton"
```

---

### Task 2: 데이터 모델

**Files:**
- Create: `src/meme_cli/models.py`
- Create: `tests/test_models.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_models.py`:
```python
from meme_cli.models import MediaItem
import uuid


def test_media_item_defaults():
    item = MediaItem(
        path="/tmp/test.gif",
        media_type="gif",
        name="테스트 밈",
        description="테스트용 GIF",
        tags=["test"],
        emotions=["neutral"],
        context=["testing"],
    )
    assert uuid.UUID(item.id)  # 유효한 UUID
    assert item.created_at != ""
    assert item.updated_at != ""


def test_media_item_roundtrip():
    item = MediaItem(
        path="/tmp/pikachu.png",
        media_type="image",
        name="놀란 피카츄",
        description="충격받은 피카츄 밈",
        tags=["pikachu", "pokemon"],
        emotions=["shocked"],
        context=["reaction", "plot-twist"],
    )
    d = item.to_dict()
    restored = MediaItem.from_dict(d)
    assert restored.id == item.id
    assert restored.tags == ["pikachu", "pokemon"]
    assert restored.emotions == ["shocked"]
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_models.py -v
```

Expected: FAIL - `ModuleNotFoundError: No module named 'meme_cli.models'`

- [ ] **Step 3: models.py 구현**

```python
from dataclasses import dataclass, field, asdict
from datetime import datetime, timezone
import uuid


@dataclass
class MediaItem:
    path: str                   # ~/.meme-cli/media/ 내 저장 경로
    media_type: str             # 'video' | 'gif' | 'image'
    name: str                   # 사람이 읽기 쉬운 이름
    description: str            # AI가 생성한 상세 설명
    tags: list[str]             # 검색용 태그
    emotions: list[str]         # 감정 태그 (shocked, happy, sad 등)
    context: list[str]          # 사용 맥락 (reaction, intro, transition 등)
    id: str = field(default_factory=lambda: str(uuid.uuid4()))
    created_at: str = field(
        default_factory=lambda: datetime.now(timezone.utc).isoformat()
    )
    updated_at: str = field(
        default_factory=lambda: datetime.now(timezone.utc).isoformat()
    )

    def to_dict(self) -> dict:
        return asdict(self)

    @classmethod
    def from_dict(cls, data: dict) -> "MediaItem":
        return cls(**data)

    def touch(self) -> None:
        """updated_at 갱신"""
        self.updated_at = datetime.now(timezone.utc).isoformat()
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_models.py -v
```

Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/models.py tests/test_models.py
git commit -m "feat: MediaItem dataclass with to_dict/from_dict"
```

---

### Task 3: SQLite 저장소 레이어

**Files:**
- Create: `src/meme_cli/store.py`
- Create: `tests/test_store.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_store.py`:
```python
import pytest
from meme_cli.store import MediaStore
from meme_cli.models import MediaItem
from pathlib import Path


@pytest.fixture
def store(tmp_store_dir):
    return MediaStore(db_path=tmp_store_dir["db"])


def make_item(**kwargs) -> MediaItem:
    defaults = dict(
        path="/tmp/test.gif",
        media_type="gif",
        name="테스트",
        description="설명",
        tags=["tag1"],
        emotions=["happy"],
        context=["reaction"],
    )
    defaults.update(kwargs)
    return MediaItem(**defaults)


def test_save_and_get(store):
    item = make_item(name="피카츄")
    store.save(item)
    fetched = store.get(item.id)
    assert fetched is not None
    assert fetched.name == "피카츄"
    assert fetched.tags == ["tag1"]


def test_get_nonexistent_returns_none(store):
    assert store.get("nonexistent-id") is None


def test_list_all(store):
    store.save(make_item(name="A"))
    store.save(make_item(name="B"))
    items = store.list_all()
    assert len(items) == 2


def test_update(store):
    item = make_item(name="원래 이름")
    store.save(item)
    item.name = "바뀐 이름"
    item.touch()
    store.update(item)
    fetched = store.get(item.id)
    assert fetched.name == "바뀐 이름"


def test_delete(store):
    item = make_item()
    store.save(item)
    store.delete(item.id)
    assert store.get(item.id) is None


def test_list_by_type(store):
    store.save(make_item(media_type="gif", name="G"))
    store.save(make_item(media_type="image", name="I"))
    gifs = store.list_all(media_type="gif")
    assert len(gifs) == 1
    assert gifs[0].name == "G"
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_store.py -v
```

Expected: FAIL - `ModuleNotFoundError: No module named 'meme_cli.store'`

- [ ] **Step 3: store.py 구현**

```python
import json
import sqlite3
from pathlib import Path
from typing import Optional

from meme_cli.models import MediaItem


CREATE_TABLE_SQL = """
CREATE TABLE IF NOT EXISTS media_items (
    id TEXT PRIMARY KEY,
    path TEXT NOT NULL,
    media_type TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT NOT NULL,
    tags TEXT NOT NULL,
    emotions TEXT NOT NULL,
    context TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
"""


class MediaStore:
    def __init__(self, db_path: Path):
        self.db_path = db_path
        self._init_db()

    def _conn(self) -> sqlite3.Connection:
        conn = sqlite3.connect(self.db_path)
        conn.row_factory = sqlite3.Row
        return conn

    def _init_db(self) -> None:
        with self._conn() as conn:
            conn.execute(CREATE_TABLE_SQL)

    def _row_to_item(self, row: sqlite3.Row) -> MediaItem:
        d = dict(row)
        d["tags"] = json.loads(d["tags"])
        d["emotions"] = json.loads(d["emotions"])
        d["context"] = json.loads(d["context"])
        return MediaItem.from_dict(d)

    def save(self, item: MediaItem) -> None:
        with self._conn() as conn:
            conn.execute(
                """INSERT INTO media_items
                   (id, path, media_type, name, description,
                    tags, emotions, context, created_at, updated_at)
                   VALUES (?,?,?,?,?,?,?,?,?,?)""",
                (
                    item.id, item.path, item.media_type, item.name,
                    item.description,
                    json.dumps(item.tags, ensure_ascii=False),
                    json.dumps(item.emotions, ensure_ascii=False),
                    json.dumps(item.context, ensure_ascii=False),
                    item.created_at, item.updated_at,
                ),
            )

    def get(self, item_id: str) -> Optional[MediaItem]:
        with self._conn() as conn:
            row = conn.execute(
                "SELECT * FROM media_items WHERE id = ?", (item_id,)
            ).fetchone()
        return self._row_to_item(row) if row else None

    def list_all(self, media_type: Optional[str] = None) -> list[MediaItem]:
        with self._conn() as conn:
            if media_type:
                rows = conn.execute(
                    "SELECT * FROM media_items WHERE media_type = ? ORDER BY created_at DESC",
                    (media_type,),
                ).fetchall()
            else:
                rows = conn.execute(
                    "SELECT * FROM media_items ORDER BY created_at DESC"
                ).fetchall()
        return [self._row_to_item(r) for r in rows]

    def update(self, item: MediaItem) -> None:
        with self._conn() as conn:
            conn.execute(
                """UPDATE media_items SET
                   name=?, description=?, tags=?, emotions=?, context=?, updated_at=?
                   WHERE id=?""",
                (
                    item.name, item.description,
                    json.dumps(item.tags, ensure_ascii=False),
                    json.dumps(item.emotions, ensure_ascii=False),
                    json.dumps(item.context, ensure_ascii=False),
                    item.updated_at, item.id,
                ),
            )

    def delete(self, item_id: str) -> None:
        with self._conn() as conn:
            conn.execute("DELETE FROM media_items WHERE id = ?", (item_id,))
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_store.py -v
```

Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/store.py tests/test_store.py
git commit -m "feat: SQLite MediaStore with CRUD operations"
```

---

### Task 4: 전문 검색 (FTS5)

**Files:**
- Create: `src/meme_cli/search.py`
- Create: `tests/test_search.py`
- Modify: `src/meme_cli/store.py` (FTS5 테이블 추가)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_search.py`:
```python
import pytest
from meme_cli.store import MediaStore
from meme_cli.search import MediaSearch
from meme_cli.models import MediaItem


@pytest.fixture
def populated_store(tmp_store_dir):
    store = MediaStore(db_path=tmp_store_dir["db"])
    items = [
        MediaItem(
            path="/tmp/pikachu.png", media_type="image",
            name="놀란 피카츄",
            description="충격받은 피카츄가 입을 벌리고 있는 장면",
            tags=["pikachu", "pokemon", "shocked"],
            emotions=["shocked", "surprised"],
            context=["reaction", "plot-twist"],
        ),
        MediaItem(
            path="/tmp/explosion.gif", media_type="gif",
            name="폭발 GIF",
            description="건물이 폭발하는 드라마틱한 장면",
            tags=["explosion", "dramatic", "fire"],
            emotions=["intense"],
            context=["transition", "action"],
        ),
        MediaItem(
            path="/tmp/laugh.mp4", media_type="video",
            name="웃음 클립",
            description="사람들이 크게 웃고 있는 장면",
            tags=["laugh", "happy", "funny"],
            emotions=["happy", "amused"],
            context=["reaction", "funny"],
        ),
    ]
    for item in items:
        store.save(item)
    return store


def test_search_by_tag(populated_store):
    search = MediaSearch(populated_store)
    results = search.query("pikachu")
    assert len(results) == 1
    assert results[0].name == "놀란 피카츄"


def test_search_by_emotion(populated_store):
    search = MediaSearch(populated_store)
    results = search.query("shocked")
    assert len(results) >= 1
    names = [r.name for r in results]
    assert "놀란 피카츄" in names


def test_search_by_description(populated_store):
    search = MediaSearch(populated_store)
    results = search.query("폭발")
    assert len(results) >= 1
    assert results[0].name == "폭발 GIF"


def test_search_filter_by_type(populated_store):
    search = MediaSearch(populated_store)
    results = search.query("reaction", media_type="gif")
    # gif 타입인 reaction만 반환
    assert all(r.media_type == "gif" for r in results)


def test_search_no_results(populated_store):
    search = MediaSearch(populated_store)
    results = search.query("존재하지않는태그xyz")
    assert results == []
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_search.py -v
```

Expected: FAIL - `ModuleNotFoundError: No module named 'meme_cli.search'`

- [ ] **Step 3: store.py에 FTS5 지원 추가**

`src/meme_cli/store.py`의 `_init_db` 메서드 뒤에 다음을 추가:

```python
CREATE_FTS_SQL = """
CREATE VIRTUAL TABLE IF NOT EXISTS media_fts USING fts5(
    id UNINDEXED,
    name,
    description,
    tags,
    emotions,
    context,
    content=media_items,
    content_rowid=rowid,
    tokenize="trigram"
);
"""

SYNC_FTS_TRIGGER_INSERT = """
CREATE TRIGGER IF NOT EXISTS media_fts_insert
AFTER INSERT ON media_items BEGIN
    INSERT INTO media_fts(rowid, id, name, description, tags, emotions, context)
    VALUES (new.rowid, new.id, new.name, new.description,
            new.tags, new.emotions, new.context);
END;
"""

SYNC_FTS_TRIGGER_DELETE = """
CREATE TRIGGER IF NOT EXISTS media_fts_delete
AFTER DELETE ON media_items BEGIN
    INSERT INTO media_fts(media_fts, rowid, id, name, description, tags, emotions, context)
    VALUES ('delete', old.rowid, old.id, old.name, old.description,
            old.tags, old.emotions, old.context);
END;
"""

SYNC_FTS_TRIGGER_UPDATE = """
CREATE TRIGGER IF NOT EXISTS media_fts_update
AFTER UPDATE ON media_items BEGIN
    INSERT INTO media_fts(media_fts, rowid, id, name, description, tags, emotions, context)
    VALUES ('delete', old.rowid, old.id, old.name, old.description,
            old.tags, old.emotions, old.context);
    INSERT INTO media_fts(rowid, id, name, description, tags, emotions, context)
    VALUES (new.rowid, new.id, new.name, new.description,
            new.tags, new.emotions, new.context);
END;
"""
```

`_init_db` 수정:
```python
def _init_db(self) -> None:
    with self._conn() as conn:
        conn.execute(CREATE_TABLE_SQL)
        conn.execute(CREATE_FTS_SQL)
        conn.execute(SYNC_FTS_TRIGGER_INSERT)
        conn.execute(SYNC_FTS_TRIGGER_DELETE)
        conn.execute(SYNC_FTS_TRIGGER_UPDATE)
```

- [ ] **Step 4: search.py 구현**

```python
from typing import Optional
from meme_cli.models import MediaItem
from meme_cli.store import MediaStore


class MediaSearch:
    def __init__(self, store: MediaStore):
        self.store = store

    def query(
        self, query: str, media_type: Optional[str] = None, limit: int = 20
    ) -> list[MediaItem]:
        """FTS5 trigram 검색. 2글자 이하는 LIKE 폴백."""
        if len(query.replace(" ", "")) < 3:
            return self._fallback_like_search(query, media_type, limit)

        with self.store._conn() as conn:
            if media_type:
                rows = conn.execute(
                    """SELECT mi.*
                       FROM media_fts
                       JOIN media_items mi ON media_fts.id = mi.id
                       WHERE media_fts MATCH ? AND mi.media_type = ?
                       ORDER BY rank
                       LIMIT ?""",
                    (query, media_type, limit),
                ).fetchall()
            else:
                rows = conn.execute(
                    """SELECT mi.*
                       FROM media_fts
                       JOIN media_items mi ON media_fts.id = mi.id
                       WHERE media_fts MATCH ?
                       ORDER BY rank
                       LIMIT ?""",
                    (query, limit),
                ).fetchall()
        return [self.store._row_to_item(r) for r in rows]

    def _fallback_like_search(
        self, query: str, media_type: Optional[str], limit: int
    ) -> list[MediaItem]:
        """2글자 이하 한국어 쿼리용 LIKE 폴백 검색"""
        pattern = f"%{query}%"
        with self.store._conn() as conn:
            if media_type:
                rows = conn.execute(
                    """SELECT * FROM media_items
                       WHERE (name LIKE ? OR description LIKE ? OR tags LIKE ?)
                       AND media_type = ? LIMIT ?""",
                    (pattern, pattern, pattern, media_type, limit),
                ).fetchall()
            else:
                rows = conn.execute(
                    """SELECT * FROM media_items
                       WHERE name LIKE ? OR description LIKE ? OR tags LIKE ?
                       LIMIT ?""",
                    (pattern, pattern, pattern, limit),
                ).fetchall()
        return [self.store._row_to_item(r) for r in rows]
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
uv run pytest tests/test_search.py -v
```

Expected: PASS (5 tests)

- [ ] **Step 6: 커밋**

```bash
git add src/meme_cli/store.py src/meme_cli/search.py tests/test_search.py
git commit -m "feat: FTS5 full-text search with auto-sync triggers"
```

---

### Task 5: AI 메타데이터 생성

**Files:**
- Create: `src/meme_cli/ai.py`
- Create: `tests/test_ai.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_ai.py`:
```python
from unittest.mock import patch, MagicMock
from pathlib import Path
from meme_cli.ai import MetadataGenerator, GeneratedMetadata


def test_generated_metadata_structure():
    meta = GeneratedMetadata(
        name="테스트",
        description="설명",
        tags=["충격", "반응"],
        emotions=["충격"],
        context=["반응"],
    )
    assert meta.name == "테스트"
    assert isinstance(meta.tags, list)


@patch("meme_cli.ai.subprocess.run")
def test_generate_calls_claude_with_path(mock_run, tmp_path):
    img_path = tmp_path / "test.png"
    img_path.write_bytes(b"\x89PNG\r\n")  # 더미 파일

    mock_run.return_value = MagicMock(
        returncode=0,
        stdout='{"name":"충격","description":"놀란 표정","tags":["충격"],"emotions":["충격"],"context":["반응"]}',
    )

    gen = MetadataGenerator()
    meta = gen.generate(img_path)

    # claude --model haiku --print 가 파일 경로를 포함한 프롬프트로 호출됐는지 확인
    call_args = mock_run.call_args
    cmd = call_args[0][0]
    assert cmd[0] == "claude"
    assert "--model" in cmd
    assert "haiku" in cmd[cmd.index("--model") + 1]
    assert "--print" in cmd
    assert str(img_path) in cmd[-1]

    assert meta.name == "충격"
    assert meta.tags == ["충격"]


@patch("meme_cli.ai.subprocess.run")
def test_parse_json_in_markdown_block(mock_run, tmp_path):
    """Claude가 ```json 블록으로 감싸 응답해도 파싱 성공"""
    img_path = tmp_path / "meme.gif"
    img_path.write_bytes(b"GIF89a")

    mock_run.return_value = MagicMock(
        returncode=0,
        stdout='```json\n{"name":"폭발","description":"폭발 장면","tags":["폭발"],"emotions":["극적"],"context":["전환"]}\n```',
    )

    gen = MetadataGenerator()
    meta = gen.generate(img_path)
    assert meta.name == "폭발"
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_ai.py -v
```

Expected: FAIL - `ModuleNotFoundError: No module named 'meme_cli.ai'`

- [ ] **Step 3: ai.py 구현**

```python
import json
import re
import subprocess
from dataclasses import dataclass
from pathlib import Path


@dataclass
class GeneratedMetadata:
    name: str
    description: str
    tags: list[str]
    emotions: list[str]
    context: list[str]


PROMPT_TEMPLATE = """{path}

위 파일을 분석해서 밈 라이브러리 검색에 최적화된 메타데이터를 JSON만 반환해 (다른 텍스트 없이):
{{
  "name": "한국어 짧은 이름 (10자 이내)",
  "description": "무슨 장면인지 상세 설명 (한국어, 2-3문장)",
  "tags": ["한국어 검색 태그 5-10개 (예: 충격, 폭발, 웃음, 피카츄)"],
  "emotions": ["한국어 감정 태그 (예: 충격, 기쁨, 슬픔, 분노, 당혹)"],
  "context": ["한국어 맥락 태그 (예: 반응, 전환, 인트로, 액션, 반전)"]
}}"""


class MetadataGenerator:
    def generate(self, file_path: Path) -> GeneratedMetadata:
        """claude --print로 파일을 분석해 메타데이터 생성"""
        prompt = PROMPT_TEMPLATE.format(path=file_path)
        result = subprocess.run(
            ["claude", "--model", "claude-haiku-4-5-20251001", "--print", prompt],
            capture_output=True,
            text=True,
            timeout=60,
        )
        if result.returncode != 0:
            raise RuntimeError(f"claude 실행 실패: {result.stderr[:200]}")
        return self._parse(result.stdout)

    def _parse(self, text: str) -> GeneratedMetadata:
        """JSON 블록 추출 — 마크다운 코드 블록 포함 처리"""
        # ```json ... ``` 또는 ``` ... ``` 제거
        text = re.sub(r"```(?:json)?\s*", "", text).strip()
        # 첫 번째 {...} 블록 추출
        match = re.search(r"\{.*\}", text, re.DOTALL)
        if not match:
            raise ValueError(f"JSON 파싱 실패. Claude 응답:\n{text[:300]}")
        data = json.loads(match.group())
        return GeneratedMetadata(
            name=data["name"],
            description=data["description"],
            tags=data.get("tags", []),
            emotions=data.get("emotions", []),
            context=data.get("context", []),
        )
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_ai.py -v
```

Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/ai.py tests/test_ai.py
git commit -m "feat: AI metadata generation via claude --print subprocess"
```

---

### Task 6: CLI - `meme add` 커맨드

**Files:**
- Modify: `src/meme_cli/cli.py`
- Create: `src/meme_cli/config.py`
- Modify: `tests/test_cli.py` (add 테스트 추가)

- [ ] **Step 1: config.py 작성 (저장소 경로 관리)**

```python
from pathlib import Path
import os


def get_base_dir() -> Path:
    """환경변수 MEME_CLI_DIR 또는 ~/.meme-cli 사용"""
    base = os.environ.get("MEME_CLI_DIR")
    if base:
        return Path(base)
    return Path.home() / ".meme-cli"


def get_db_path() -> Path:
    d = get_base_dir()
    d.mkdir(parents=True, exist_ok=True)
    return d / "memes.db"


def get_media_dir() -> Path:
    d = get_base_dir() / "media"
    d.mkdir(parents=True, exist_ok=True)
    return d
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_cli.py` (신규 생성):
```python
import pytest
import json
from pathlib import Path
from typer.testing import CliRunner
from unittest.mock import patch, MagicMock

from meme_cli.cli import app

runner = CliRunner()


@pytest.fixture
def env_vars(tmp_store_dir, monkeypatch):
    monkeypatch.setenv("MEME_CLI_DIR", str(tmp_store_dir["base"]))
    return tmp_store_dir


@pytest.fixture
def sample_image(tmp_path):
    img_path = tmp_path / "pikachu.png"
    img_path.write_bytes(b"\x89PNG\r\n\x1a\n")  # 더미 PNG (PIL 불필요)
    return img_path


@patch("meme_cli.cli.MetadataGenerator")
def test_add_command(mock_gen_class, env_vars, sample_image):
    mock_gen = MagicMock()
    mock_gen_class.return_value = mock_gen
    mock_gen.generate.return_value = MagicMock(
        name="노란 이미지",
        description="노란색 배경",
        tags=["yellow"],
        emotions=["neutral"],
        context=["placeholder"],
    )

    result = runner.invoke(app, ["add", str(sample_image)])
    assert result.exit_code == 0
    assert "추가됨" in result.output or "Added" in result.output or "노란 이미지" in result.output


@patch("meme_cli.cli.MetadataGenerator")
def test_add_copies_file_to_store(mock_gen_class, env_vars, sample_image):
    mock_gen = MagicMock()
    mock_gen_class.return_value = mock_gen
    mock_gen.generate.return_value = MagicMock(
        name="테스트", description="설명", tags=[], emotions=[], context=[]
    )

    runner.invoke(app, ["add", str(sample_image)])

    media_dir = Path(env_vars["base"]) / "media"
    stored_files = list(media_dir.iterdir())
    assert len(stored_files) == 1
    assert stored_files[0].suffix == ".png"
```

- [ ] **Step 3: 실패 확인**

```bash
uv run pytest tests/test_cli.py::test_add_command tests/test_cli.py::test_add_copies_file_to_store -v
```

Expected: FAIL

- [ ] **Step 4: cli.py에 add 커맨드 구현**

`src/meme_cli/cli.py` 전체 교체:
```python
import json
import shutil
import uuid
from pathlib import Path
from typing import Optional

import typer
from rich.console import Console
from rich.table import Table
from rich.panel import Panel

from meme_cli.ai import MetadataGenerator
from meme_cli.config import get_db_path, get_media_dir
from meme_cli.models import MediaItem
from meme_cli.search import MediaSearch
from meme_cli.store import MediaStore

app = typer.Typer(help="밈 미디어 관리 CLI", no_args_is_help=True)
console = Console()


def _get_store() -> MediaStore:
    return MediaStore(db_path=get_db_path())


def _detect_type(path: Path) -> str:
    suffix = path.suffix.lower()
    if suffix in (".mp4", ".mov", ".avi", ".mkv", ".webm"):
        return "video"
    if suffix == ".gif":
        return "gif"
    return "image"


@app.command()
def add(
    file: Path = typer.Argument(..., help="추가할 미디어 파일 경로"),
    name: Optional[str] = typer.Option(None, "--name", "-n", help="사용자 지정 이름 (없으면 AI가 생성)"),
):
    """미디어 파일을 저장소에 추가하고 AI로 메타데이터 생성"""
    if not file.exists():
        console.print(f"[red]파일을 찾을 수 없습니다: {file}[/red]")
        raise typer.Exit(1)

    # 미디어 디렉토리에 복사
    media_dir = get_media_dir()
    dest = media_dir / f"{uuid.uuid4()}{file.suffix}"
    shutil.copy2(file, dest)

    console.print(f"[dim]AI 메타데이터 생성 중...[/dim]")
    gen = MetadataGenerator()
    meta = gen.generate(dest)

    item = MediaItem(
        path=str(dest),
        media_type=_detect_type(file),
        name=name or meta.name,
        description=meta.description,
        tags=meta.tags,
        emotions=meta.emotions,
        context=meta.context,
    )

    store = _get_store()
    store.save(item)

    console.print(Panel(
        f"[bold green]추가됨[/bold green]\n"
        f"ID: {item.id}\n"
        f"이름: {item.name}\n"
        f"태그: {', '.join(item.tags)}\n"
        f"감정: {', '.join(item.emotions)}\n"
        f"맥락: {', '.join(item.context)}",
        title="밈 추가 완료"
    ))
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
uv run pytest tests/test_cli.py::test_add_command tests/test_cli.py::test_add_copies_file_to_store -v
```

Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add src/meme_cli/cli.py src/meme_cli/config.py tests/test_cli.py
git commit -m "feat: meme add command with AI metadata generation"
```

---

### Task 7: CLI - `meme search` 커맨드

**Files:**
- Modify: `src/meme_cli/cli.py`
- Modify: `tests/test_cli.py`

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_cli.py`에 추가:
```python
from meme_cli.store import MediaStore


@pytest.fixture
def store_with_items(env_vars):
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    items = [
        MediaItem(
            path="/tmp/a.gif", media_type="gif",
            name="폭발", description="폭발 장면", tags=["explosion"],
            emotions=["intense"], context=["action"],
        ),
        MediaItem(
            path="/tmp/b.png", media_type="image",
            name="충격", description="충격받은 표정", tags=["shocked"],
            emotions=["shocked"], context=["reaction"],
        ),
    ]
    for item in items:
        store.save(item)
    return store


def test_search_command_text_output(store_with_items, env_vars):
    result = runner.invoke(app, ["search", "explosion"])
    assert result.exit_code == 0
    assert "폭발" in result.output


def test_search_command_json_output(store_with_items, env_vars):
    result = runner.invoke(app, ["search", "shocked", "--json"])
    assert result.exit_code == 0
    data = json.loads(result.output)
    assert isinstance(data, list)
    assert data[0]["name"] == "충격"
    assert "path" in data[0]


def test_search_command_type_filter(store_with_items, env_vars):
    result = runner.invoke(app, ["search", "reaction", "--type", "image", "--json"])
    assert result.exit_code == 0
    data = json.loads(result.output)
    assert all(d["media_type"] == "image" for d in data)


def test_search_no_results(store_with_items, env_vars):
    result = runner.invoke(app, ["search", "존재안함xyz"])
    assert result.exit_code == 0
    assert "결과 없음" in result.output or "No results" in result.output
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_cli.py::test_search_command_text_output tests/test_cli.py::test_search_command_json_output -v
```

Expected: FAIL

- [ ] **Step 3: search 커맨드 구현** (`src/meme_cli/cli.py`에 추가)

```python
@app.command()
def search(
    query: str = typer.Argument(..., help="검색 쿼리"),
    media_type: Optional[str] = typer.Option(None, "--type", "-t", help="타입 필터: video|gif|image"),
    limit: int = typer.Option(20, "--limit", "-l", help="최대 결과 수"),
    output_json: bool = typer.Option(False, "--json", help="JSON 형식으로 출력 (AI 사용 권장)"),
):
    """메타데이터 기반 밈 검색"""
    store = _get_store()
    searcher = MediaSearch(store)
    results = searcher.query(query, media_type=media_type, limit=limit)

    if output_json:
        console.print(json.dumps([r.to_dict() for r in results], ensure_ascii=False, indent=2))
        return

    if not results:
        console.print("[yellow]결과 없음[/yellow]")
        return

    table = Table(title=f"검색 결과: '{query}'")
    table.add_column("ID", style="dim", width=8)
    table.add_column("이름", style="bold")
    table.add_column("타입")
    table.add_column("태그")
    table.add_column("감정")

    for r in results:
        table.add_row(
            r.id[:8],
            r.name,
            r.media_type,
            ", ".join(r.tags[:4]),
            ", ".join(r.emotions[:3]),
        )

    console.print(table)
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_cli.py -k "search" -v
```

Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/cli.py tests/test_cli.py
git commit -m "feat: meme search command with JSON output for AI integration"
```

---

### Task 8: CLI - `meme edit` 커맨드 (대화형 메타데이터 수정)

**Files:**
- Modify: `src/meme_cli/cli.py`
- Modify: `tests/test_cli.py`

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_cli.py`에 추가:
```python
def test_edit_command_name(store_with_items, env_vars):
    # store에서 첫 번째 아이템 ID 가져오기
    from meme_cli.store import MediaStore
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    items = store.list_all()
    item_id = items[0].id

    result = runner.invoke(app, ["edit", item_id, "--name", "새 이름"])
    assert result.exit_code == 0

    updated = store.get(item_id)
    assert updated.name == "새 이름"


def test_edit_command_tags(store_with_items, env_vars):
    from meme_cli.store import MediaStore
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    items = store.list_all()
    item_id = items[0].id

    result = runner.invoke(app, ["edit", item_id, "--tags", "new-tag,another-tag"])
    assert result.exit_code == 0

    updated = store.get(item_id)
    assert "new-tag" in updated.tags
    assert "another-tag" in updated.tags


def test_edit_nonexistent(env_vars):
    result = runner.invoke(app, ["edit", "nonexistent-id", "--name", "test"])
    assert result.exit_code != 0
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_cli.py -k "edit" -v
```

Expected: FAIL

- [ ] **Step 3: edit 커맨드 구현** (`src/meme_cli/cli.py`에 추가)

```python
@app.command()
def edit(
    item_id: str = typer.Argument(..., help="수정할 아이템 ID (또는 ID 앞 8자)"),
    name: Optional[str] = typer.Option(None, "--name", help="새 이름"),
    description: Optional[str] = typer.Option(None, "--desc", help="새 설명"),
    tags: Optional[str] = typer.Option(None, "--tags", help="새 태그 (쉼표 구분)"),
    emotions: Optional[str] = typer.Option(None, "--emotions", help="새 감정 태그 (쉼표 구분)"),
    context: Optional[str] = typer.Option(None, "--context", help="새 맥락 태그 (쉼표 구분)"),
    interactive: bool = typer.Option(False, "--interactive", "-i", help="대화형 수정 모드"),
):
    """메타데이터 수정"""
    store = _get_store()

    # ID 접두어로 검색 지원
    item = store.get(item_id)
    if item is None:
        all_items = store.list_all()
        matches = [i for i in all_items if i.id.startswith(item_id)]
        if len(matches) == 1:
            item = matches[0]
        elif len(matches) > 1:
            console.print(f"[red]ID 접두어 '{item_id}'에 여러 항목이 일치합니다. 더 긴 ID를 입력하세요.[/red]")
            raise typer.Exit(1)
        else:
            console.print(f"[red]ID '{item_id}'를 찾을 수 없습니다.[/red]")
            raise typer.Exit(1)

    if interactive:
        console.print(Panel(str(item.to_dict()), title="현재 메타데이터"))
        name = typer.prompt("이름", default=item.name)
        description = typer.prompt("설명", default=item.description)
        tags = typer.prompt("태그 (쉼표 구분)", default=",".join(item.tags))
        emotions = typer.prompt("감정 (쉼표 구분)", default=",".join(item.emotions))
        context = typer.prompt("맥락 (쉼표 구분)", default=",".join(item.context))

    if name is not None:
        item.name = name
    if description is not None:
        item.description = description
    if tags is not None:
        item.tags = [t.strip() for t in tags.split(",") if t.strip()]
    if emotions is not None:
        item.emotions = [e.strip() for e in emotions.split(",") if e.strip()]
    if context is not None:
        item.context = [c.strip() for c in context.split(",") if c.strip()]

    item.touch()
    store.update(item)
    console.print(f"[green]수정됨: {item.name} ({item.id[:8]})[/green]")
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_cli.py -k "edit" -v
```

Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/cli.py tests/test_cli.py
git commit -m "feat: meme edit command with flag-based and interactive modes"
```

---

### Task 9: CLI - `list`, `get`, `remove` 커맨드

**Files:**
- Modify: `src/meme_cli/cli.py`
- Modify: `tests/test_cli.py`

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_cli.py`에 추가:
```python
def test_list_command(store_with_items, env_vars):
    result = runner.invoke(app, ["list"])
    assert result.exit_code == 0
    assert "폭발" in result.output
    assert "충격" in result.output


def test_list_json(store_with_items, env_vars):
    result = runner.invoke(app, ["list", "--json"])
    assert result.exit_code == 0
    data = json.loads(result.output)
    assert len(data) == 2


def test_get_command(store_with_items, env_vars):
    from meme_cli.store import MediaStore
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    item = store.list_all()[0]

    result = runner.invoke(app, ["get", item.id])
    assert result.exit_code == 0
    assert item.name in result.output


def test_get_json(store_with_items, env_vars):
    from meme_cli.store import MediaStore
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    item = store.list_all()[0]

    result = runner.invoke(app, ["get", item.id, "--json"])
    assert result.exit_code == 0
    data = json.loads(result.output)
    assert data["id"] == item.id
    assert "path" in data


def test_remove_command(store_with_items, env_vars):
    from meme_cli.store import MediaStore
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    item = store.list_all()[0]

    result = runner.invoke(app, ["remove", item.id, "--yes"])
    assert result.exit_code == 0
    assert store.get(item.id) is None
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_cli.py -k "test_list or test_get or test_remove" -v
```

Expected: FAIL

- [ ] **Step 3: list/get/remove 커맨드 구현** (`src/meme_cli/cli.py`에 추가)

```python
@app.command(name="list")
def list_items(
    media_type: Optional[str] = typer.Option(None, "--type", "-t", help="타입 필터: video|gif|image"),
    output_json: bool = typer.Option(False, "--json", help="JSON 출력"),
):
    """저장된 모든 밈 목록 보기"""
    store = _get_store()
    items = store.list_all(media_type=media_type)

    if output_json:
        console.print(json.dumps([i.to_dict() for i in items], ensure_ascii=False, indent=2))
        return

    if not items:
        console.print("[yellow]저장된 밈이 없습니다. 'meme add <파일>'로 추가하세요.[/yellow]")
        return

    table = Table(title="밈 목록")
    table.add_column("ID", style="dim", width=8)
    table.add_column("이름", style="bold")
    table.add_column("타입")
    table.add_column("태그")
    table.add_column("감정")
    table.add_column("날짜")

    for i in items:
        table.add_row(
            i.id[:8], i.name, i.media_type,
            ", ".join(i.tags[:3]),
            ", ".join(i.emotions[:2]),
            i.created_at[:10],
        )

    console.print(table)


@app.command()
def get(
    item_id: str = typer.Argument(..., help="아이템 ID"),
    output_json: bool = typer.Option(False, "--json", help="JSON 출력"),
):
    """특정 밈의 상세 정보 조회"""
    store = _get_store()
    item = store.get(item_id)

    if item is None:
        # 접두어 검색
        all_items = store.list_all()
        matches = [i for i in all_items if i.id.startswith(item_id)]
        if len(matches) == 1:
            item = matches[0]
        else:
            console.print(f"[red]ID '{item_id}'를 찾을 수 없습니다.[/red]")
            raise typer.Exit(1)

    if output_json:
        console.print(json.dumps(item.to_dict(), ensure_ascii=False, indent=2))
        return

    console.print(Panel(
        f"[bold]ID:[/bold] {item.id}\n"
        f"[bold]이름:[/bold] {item.name}\n"
        f"[bold]타입:[/bold] {item.media_type}\n"
        f"[bold]경로:[/bold] {item.path}\n"
        f"[bold]설명:[/bold] {item.description}\n"
        f"[bold]태그:[/bold] {', '.join(item.tags)}\n"
        f"[bold]감정:[/bold] {', '.join(item.emotions)}\n"
        f"[bold]맥락:[/bold] {', '.join(item.context)}\n"
        f"[bold]추가일:[/bold] {item.created_at[:10]}",
        title=item.name,
    ))


@app.command()
def remove(
    item_id: str = typer.Argument(..., help="삭제할 아이템 ID"),
    yes: bool = typer.Option(False, "--yes", "-y", help="확인 없이 삭제"),
    keep_file: bool = typer.Option(False, "--keep-file", help="원본 파일 유지"),
):
    """저장소에서 밈 삭제"""
    store = _get_store()
    item = store.get(item_id)

    if item is None:
        all_items = store.list_all()
        matches = [i for i in all_items if i.id.startswith(item_id)]
        if len(matches) == 1:
            item = matches[0]
        else:
            console.print(f"[red]ID '{item_id}'를 찾을 수 없습니다.[/red]")
            raise typer.Exit(1)

    if not yes:
        confirm = typer.confirm(f"'{item.name}'을 삭제하시겠습니까?")
        if not confirm:
            raise typer.Abort()

    if not keep_file:
        file_path = Path(item.path)
        if file_path.exists():
            file_path.unlink()

    store.delete(item.id)
    console.print(f"[green]삭제됨: {item.name}[/green]")
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_cli.py -k "test_list or test_get or test_remove" -v
```

Expected: PASS (5 tests)

- [ ] **Step 5: 전체 테스트 통과 확인**

```bash
uv run pytest tests/ -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 6: 커밋**

```bash
git add src/meme_cli/cli.py tests/test_cli.py
git commit -m "feat: list/get/remove commands - complete CLI interface"
```

---

### Task 10: README 및 최종 통합 검증

**Files:**
- Create: `README.md`

- [ ] **Step 1: 설치 및 동작 확인**

```bash
uv run meme --help
uv run meme add --help
uv run meme search --help
uv run meme edit --help
uv run meme list --help
uv run meme get --help
uv run meme remove --help
```

Expected: 모든 커맨드 help 출력됨

- [ ] **Step 2: ANTHROPIC_API_KEY 설정 확인**

```bash
echo $ANTHROPIC_API_KEY
```

Expected: API 키 출력됨 (없으면 `export ANTHROPIC_API_KEY=sk-ant-...` 필요)

- [ ] **Step 3: 실제 미디어 파일로 엔드-투-엔드 테스트**

```bash
# 테스트용 이미지 다운로드 또는 직접 준비 후:
uv run meme add /path/to/test-meme.gif
uv run meme list
uv run meme search "shocked"
uv run meme search "reaction" --json
```

Expected: 올바른 메타데이터 생성 및 검색 결과 반환

- [ ] **Step 4: 최종 커밋**

```bash
git add .
git commit -m "docs: README with usage guide and AI integration examples"
```

---

## AI 통합 사용 예시

AI 영상 제작 시스템이 이 CLI를 사용하는 방법:

```bash
# 1. 사용 가능한 반응 밈 검색 (JSON 출력)
meme search "shocked reaction" --json

# 2. 특정 타입만 검색
meme search "funny" --type gif --json --limit 5

# 3. 선택한 밈 상세 조회
meme get <id> --json

# 응답 예시:
# {
#   "id": "uuid",
#   "name": "놀란 피카츄",
#   "path": "/Users/user/.meme-cli/media/uuid.png",
#   "media_type": "image",
#   "description": "충격받은 피카츄가 입을 벌리고 있는 장면",
#   "tags": ["pikachu", "pokemon", "shocked"],
#   "emotions": ["shocked", "surprised"],
#   "context": ["reaction", "plot-twist"]
# }
```

---

---

## Phase 2: 유사 의미 검색 (Semantic Search)

**추가 스택:** sentence-transformers (BAAI/bge-m3), ChromaDB

**아키텍처:**
- SQLite FTS5: 기존 정확 매칭 검색 유지
- ChromaDB (`~/.meme-cli/chroma/`): 임베딩 벡터 저장
- `meme add` → 메타데이터 임베딩 → ChromaDB 저장
- `meme edit` → 임베딩 재생성
- `meme remove` → ChromaDB에서도 삭제
- `meme search --semantic` → 벡터 유사도 검색

**변경 파일 전체 목록:**
- Modify: `pyproject.toml` — 의존성 추가
- Modify: `src/meme_cli/config.py` — `get_chroma_dir()` 추가
- Create: `src/meme_cli/embeddings.py` — EmbeddingStore 클래스
- Modify: `src/meme_cli/search.py` — `semantic_query()` 추가
- Modify: `src/meme_cli/cli.py` — add/edit/remove/search 커맨드 수정
- Create: `tests/test_embeddings.py`
- Modify: `tests/test_cli.py` — add/edit/remove mock 업데이트, semantic 테스트 추가

---

### Task 11: 의존성 추가 및 config 업데이트

**Files:**
- Modify: `pyproject.toml`
- Modify: `src/meme_cli/config.py`

- [ ] **Step 1: pyproject.toml에 의존성 추가**

```toml
dependencies = [
    "typer>=0.12",
    "rich>=13",
    "sentence-transformers>=3.0",
    "chromadb>=0.5",
]
```

- [ ] **Step 2: uv sync 실행**

```bash
uv sync
```

Expected: sentence-transformers, chromadb, torch 설치 완료 (수 분 소요)

- [ ] **Step 3: config.py에 get_chroma_dir 추가**

기존 `get_media_dir()` 함수 뒤에 추가:

```python
def get_chroma_dir() -> Path:
    d = get_base_dir() / "chroma"
    d.mkdir(parents=True, exist_ok=True)
    return d
```

- [ ] **Step 4: import 확인**

```bash
uv run python -c "import chromadb; import sentence_transformers; print('OK')"
```

Expected: `OK`

- [ ] **Step 5: 커밋**

```bash
git add pyproject.toml src/meme_cli/config.py
git commit -m "chore: add sentence-transformers and chromadb dependencies"
```

---

### Task 12: EmbeddingStore 구현

**Files:**
- Create: `src/meme_cli/embeddings.py`
- Create: `tests/test_embeddings.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_embeddings.py`:
```python
import pytest
import numpy as np
from unittest.mock import MagicMock, patch
from pathlib import Path
from meme_cli.embeddings import EmbeddingStore, metadata_to_text
from meme_cli.models import MediaItem


def make_item(**kwargs) -> MediaItem:
    defaults = dict(
        path="/tmp/test.gif", media_type="gif",
        name="충격 밈", description="충격받은 표정",
        tags=["충격", "반응"], emotions=["충격"], context=["반응"],
    )
    defaults.update(kwargs)
    return MediaItem(**defaults)


def test_metadata_to_text():
    item = make_item()
    text = metadata_to_text(item)
    assert "충격 밈" in text
    assert "충격받은 표정" in text
    assert "충격" in text


@pytest.fixture
def mock_store(tmp_path):
    """chromadb와 SentenceTransformer를 mock한 EmbeddingStore"""
    with patch("meme_cli.embeddings.chromadb") as mock_chroma:
        mock_collection = MagicMock()
        mock_chroma.PersistentClient.return_value.get_or_create_collection.return_value = mock_collection
        store = EmbeddingStore(chroma_dir=tmp_path / "chroma")
        # 모델 lazy load 우회
        store._model = MagicMock()
        store._model.encode.return_value = np.ones(1024, dtype=np.float32)
        yield store, mock_collection


def test_upsert_calls_collection(mock_store):
    store, mock_collection = mock_store
    item = make_item()
    store.upsert(item)
    mock_collection.upsert.assert_called_once()
    call_kwargs = mock_collection.upsert.call_args[1]
    assert call_kwargs["ids"] == [item.id]
    assert "embeddings" in call_kwargs
    assert call_kwargs["metadatas"][0]["media_type"] == "gif"


def test_delete_calls_collection(mock_store):
    store, mock_collection = mock_store
    store.delete("some-id")
    mock_collection.delete.assert_called_once_with(ids=["some-id"])


def test_query_returns_ids(mock_store):
    store, mock_collection = mock_store
    mock_collection.query.return_value = {"ids": [["id-1", "id-2"]]}
    result = store.query("충격")
    assert result == ["id-1", "id-2"]


def test_query_with_media_type_filter(mock_store):
    store, mock_collection = mock_store
    mock_collection.query.return_value = {"ids": [["id-1"]]}
    store.query("충격", media_type="gif")
    call_kwargs = mock_collection.query.call_args[1]
    assert call_kwargs["where"] == {"media_type": "gif"}


def test_query_without_filter_has_no_where(mock_store):
    store, mock_collection = mock_store
    mock_collection.query.return_value = {"ids": [[]]}
    store.query("충격")
    call_kwargs = mock_collection.query.call_args[1]
    assert "where" not in call_kwargs
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_embeddings.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'meme_cli.embeddings'`

- [ ] **Step 3: embeddings.py 구현**

```python
from pathlib import Path
from typing import Optional

from meme_cli.models import MediaItem

MODEL_NAME = "BAAI/bge-m3"


def metadata_to_text(item: MediaItem) -> str:
    """MediaItem 메타데이터를 임베딩용 단일 텍스트로 변환"""
    return " ".join([
        item.name,
        item.description,
        " ".join(item.tags),
        " ".join(item.emotions),
        " ".join(item.context),
    ])


class EmbeddingStore:
    def __init__(self, chroma_dir: Path):
        import chromadb
        self.client = chromadb.PersistentClient(path=str(chroma_dir))
        self.collection = self.client.get_or_create_collection(
            "memes",
            metadata={"hnsw:space": "cosine"},
        )
        self._model = None  # lazy load (첫 호출 시 다운로드)

    @property
    def model(self):
        if self._model is None:
            from sentence_transformers import SentenceTransformer
            self._model = SentenceTransformer(MODEL_NAME)
        return self._model

    def upsert(self, item: MediaItem) -> None:
        """아이템 추가 또는 업데이트 (edit 시 재사용)"""
        text = metadata_to_text(item)
        embedding = self.model.encode(text, normalize_embeddings=True).tolist()
        self.collection.upsert(
            ids=[item.id],
            embeddings=[embedding],
            metadatas=[{"media_type": item.media_type}],
        )

    def delete(self, item_id: str) -> None:
        self.collection.delete(ids=[item_id])

    def query(
        self,
        query_text: str,
        media_type: Optional[str] = None,
        n_results: int = 20,
    ) -> list[str]:
        """유사도 높은 순서로 item ID 목록 반환"""
        embedding = self.model.encode(query_text, normalize_embeddings=True).tolist()
        query_params: dict = {
            "query_embeddings": [embedding],
            "n_results": n_results,
        }
        if media_type:
            query_params["where"] = {"media_type": media_type}
        results = self.collection.query(**query_params)
        return results["ids"][0] if results["ids"] else []
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_embeddings.py -v
```

Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/embeddings.py tests/test_embeddings.py
git commit -m "feat: EmbeddingStore with ChromaDB and bge-m3 (lazy loaded)"
```

---

### Task 13: search.py에 semantic_query 추가

**Files:**
- Modify: `src/meme_cli/search.py`
- Modify: `tests/test_search.py`

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_search.py` 끝에 추가:

```python
from unittest.mock import MagicMock
from meme_cli.embeddings import EmbeddingStore


def test_semantic_query_returns_items(populated_store):
    mock_emb = MagicMock(spec=EmbeddingStore)
    # 피카츄 ID 반환하도록 mock
    pikachu = [i for i in populated_store.list_all() if i.name == "놀란 피카츄"][0]
    mock_emb.query.return_value = [pikachu.id]

    search = MediaSearch(populated_store, embedding_store=mock_emb)
    results = search.semantic_query("충격받은 표정")

    assert len(results) == 1
    assert results[0].name == "놀란 피카츄"
    mock_emb.query.assert_called_once_with("충격받은 표정", media_type=None, n_results=40)


def test_semantic_query_type_filter(populated_store):
    mock_emb = MagicMock(spec=EmbeddingStore)
    gif = [i for i in populated_store.list_all() if i.media_type == "gif"][0]
    mock_emb.query.return_value = [gif.id]

    search = MediaSearch(populated_store, embedding_store=mock_emb)
    results = search.semantic_query("폭발", media_type="gif")

    mock_emb.query.assert_called_once_with("폭발", media_type="gif", n_results=40)
    assert all(r.media_type == "gif" for r in results)


def test_semantic_query_skips_missing_ids(populated_store):
    """ChromaDB에는 있지만 SQLite에서 삭제된 ID는 무시"""
    mock_emb = MagicMock(spec=EmbeddingStore)
    mock_emb.query.return_value = ["nonexistent-id", populated_store.list_all()[0].id]

    search = MediaSearch(populated_store, embedding_store=mock_emb)
    results = search.semantic_query("테스트")
    assert len(results) == 1


def test_semantic_query_raises_without_embedding_store(populated_store):
    search = MediaSearch(populated_store)  # embedding_store 없음
    with pytest.raises(RuntimeError, match="임베딩 저장소"):
        search.semantic_query("테스트")
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_search.py -v
```

Expected: FAIL (새 4개 테스트)

- [ ] **Step 3: search.py 수정**

```python
from typing import Optional
from meme_cli.models import MediaItem
from meme_cli.store import MediaStore

# TYPE_CHECKING으로 순환 import 방지
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from meme_cli.embeddings import EmbeddingStore


class MediaSearch:
    def __init__(self, store: MediaStore, embedding_store: "Optional[EmbeddingStore]" = None):
        self.store = store
        self.embedding_store = embedding_store

    def query(
        self, query: str, media_type: Optional[str] = None, limit: int = 20
    ) -> list[MediaItem]:
        """FTS5 trigram 검색. 2글자 이하는 LIKE 폴백."""
        if len(query.replace(" ", "")) < 3:
            return self._fallback_like_search(query, media_type, limit)

        with self.store._conn() as conn:
            if media_type:
                rows = conn.execute(
                    """SELECT mi.*
                       FROM media_fts
                       JOIN media_items mi ON media_fts.id = mi.id
                       WHERE media_fts MATCH ? AND mi.media_type = ?
                       ORDER BY rank
                       LIMIT ?""",
                    (query, media_type, limit),
                ).fetchall()
            else:
                rows = conn.execute(
                    """SELECT mi.*
                       FROM media_fts
                       JOIN media_items mi ON media_fts.id = mi.id
                       WHERE media_fts MATCH ?
                       ORDER BY rank
                       LIMIT ?""",
                    (query, limit),
                ).fetchall()
        return [self.store._row_to_item(r) for r in rows]

    def semantic_query(
        self, query: str, media_type: Optional[str] = None, limit: int = 20
    ) -> list[MediaItem]:
        """벡터 유사도 기반 의미 검색"""
        if not self.embedding_store:
            raise RuntimeError("임베딩 저장소가 초기화되지 않았습니다.")
        # n_results를 limit*2로 요청해 media_type 필터 후 부족분을 줄임
        ids = self.embedding_store.query(query, media_type=media_type, n_results=limit * 2)
        items = [self.store.get(id) for id in ids]
        # SQLite에서 삭제됐지만 ChromaDB에 남은 항목 제거
        return [i for i in items if i is not None][:limit]

    def _fallback_like_search(
        self, query: str, media_type: Optional[str], limit: int
    ) -> list[MediaItem]:
        """2글자 이하 한국어 쿼리용 LIKE 폴백 검색"""
        pattern = f"%{query}%"
        with self.store._conn() as conn:
            if media_type:
                rows = conn.execute(
                    """SELECT * FROM media_items
                       WHERE (name LIKE ? OR description LIKE ? OR tags LIKE ?)
                       AND media_type = ? LIMIT ?""",
                    (pattern, pattern, pattern, media_type, limit),
                ).fetchall()
            else:
                rows = conn.execute(
                    """SELECT * FROM media_items
                       WHERE name LIKE ? OR description LIKE ? OR tags LIKE ?
                       LIMIT ?""",
                    (pattern, pattern, pattern, limit),
                ).fetchall()
        return [self.store._row_to_item(r) for r in rows]
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
uv run pytest tests/test_search.py -v
```

Expected: PASS (10 tests — 기존 6 + 신규 4)

- [ ] **Step 5: 커밋**

```bash
git add src/meme_cli/search.py tests/test_search.py
git commit -m "feat: semantic_query via EmbeddingStore in MediaSearch"
```

---

### Task 14: CLI 커맨드 업데이트 (add/edit/remove/search)

**Files:**
- Modify: `src/meme_cli/cli.py`
- Modify: `tests/test_cli.py`

변경 포인트 4곳:
1. `add`: 저장 후 `emb_store.upsert(item)` 호출
2. `edit`: 업데이트 후 `emb_store.upsert(item)` 호출
3. `remove`: 삭제 후 `emb_store.delete(item.id)` 호출
4. `search`: `--semantic` 플래그 추가

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/test_cli.py`에 추가:

```python
from meme_cli.embeddings import EmbeddingStore


@patch("meme_cli.cli.EmbeddingStore")
@patch("meme_cli.cli.MetadataGenerator")
def test_add_calls_embedding_upsert(mock_gen_class, mock_emb_class, env_vars, sample_image):
    mock_gen = MagicMock()
    mock_gen_class.return_value = mock_gen
    meta = MagicMock()
    meta.name = "테스트"
    meta.description = "설명"
    meta.tags = ["태그"]
    meta.emotions = ["감정"]
    meta.context = ["맥락"]
    mock_gen.generate.return_value = meta

    mock_emb = MagicMock()
    mock_emb_class.return_value = mock_emb

    runner.invoke(app, ["add", str(sample_image)])
    mock_emb.upsert.assert_called_once()


@patch("meme_cli.cli.EmbeddingStore")
def test_edit_calls_embedding_upsert(mock_emb_class, store_with_items, env_vars):
    mock_emb = MagicMock()
    mock_emb_class.return_value = mock_emb

    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    item_id = store.list_all()[0].id

    runner.invoke(app, ["edit", item_id, "--name", "수정된 이름"])
    mock_emb.upsert.assert_called_once()


@patch("meme_cli.cli.EmbeddingStore")
def test_remove_calls_embedding_delete(mock_emb_class, store_with_items, env_vars):
    mock_emb = MagicMock()
    mock_emb_class.return_value = mock_emb

    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    item_id = store.list_all()[0].id

    runner.invoke(app, ["remove", item_id, "--yes"])
    mock_emb.delete.assert_called_once_with(item_id)


@patch("meme_cli.cli.EmbeddingStore")
def test_search_semantic_flag(mock_emb_class, store_with_items, env_vars):
    mock_emb = MagicMock()
    mock_emb_class.return_value = mock_emb
    # 유사도 검색 결과로 충격 아이템 반환
    store = MediaStore(db_path=Path(env_vars["base"]) / "memes.db")
    충격_id = next(i.id for i in store.list_all() if i.name == "충격")
    mock_emb.query.return_value = [충격_id]

    result = runner.invoke(app, ["search", "슬픔", "--semantic"])
    assert result.exit_code == 0
    assert "충격" in result.output
```

- [ ] **Step 2: 실패 확인**

```bash
uv run pytest tests/test_cli.py -k "embedding or semantic" -v
```

Expected: FAIL

- [ ] **Step 3: cli.py 수정 — import 추가**

기존 import 블록에 추가:

```python
from meme_cli.embeddings import EmbeddingStore
from meme_cli.config import get_db_path, get_media_dir, get_chroma_dir
```

`_get_store()` 아래에 추가:

```python
def _get_emb_store() -> EmbeddingStore:
    return EmbeddingStore(chroma_dir=get_chroma_dir())
```

- [ ] **Step 4: add 커맨드에 upsert 추가**

`store.save(item)` 호출 직후:

```python
    store.save(item)

    console.print("[dim]임베딩 생성 중...[/dim]")
    emb_store = _get_emb_store()
    emb_store.upsert(item)
```

- [ ] **Step 5: edit 커맨드에 upsert 추가**

`store.update(item)` 호출 직후:

```python
    store.update(item)
    emb_store = _get_emb_store()
    emb_store.upsert(item)
    console.print(f"[green]수정됨: {item.name} ({item.id[:8]})[/green]")
```

- [ ] **Step 6: remove 커맨드에 delete 추가**

`store.delete(item.id)` 호출 직후:

```python
    store.delete(item.id)
    emb_store = _get_emb_store()
    emb_store.delete(item.id)
    console.print(f"[green]삭제됨: {item.name}[/green]")
```

- [ ] **Step 7: search 커맨드에 --semantic 플래그 추가**

```python
@app.command()
def search(
    query: str = typer.Argument(..., help="검색 쿼리"),
    media_type: Optional[str] = typer.Option(None, "--type", "-t", help="타입 필터: video|gif|image"),
    limit: int = typer.Option(20, "--limit", "-l", help="최대 결과 수"),
    output_json: bool = typer.Option(False, "--json", help="JSON 형식으로 출력 (AI 사용 권장)"),
    semantic: bool = typer.Option(False, "--semantic", "-s", help="유사 의미 검색 (벡터 유사도)"),
):
    """메타데이터 기반 밈 검색"""
    store = _get_store()

    if semantic:
        emb_store = _get_emb_store()
        searcher = MediaSearch(store, embedding_store=emb_store)
        results = searcher.semantic_query(query, media_type=media_type, limit=limit)
    else:
        searcher = MediaSearch(store)
        results = searcher.query(query, media_type=media_type, limit=limit)

    if output_json:
        console.print(json.dumps([r.to_dict() for r in results], ensure_ascii=False, indent=2))
        return

    if not results:
        console.print("[yellow]결과 없음[/yellow]")
        return

    mode_label = "[cyan]의미 검색[/cyan]" if semantic else "키워드 검색"
    table = Table(title=f"{mode_label}: '{query}'")
    table.add_column("ID", style="dim", width=8)
    table.add_column("이름", style="bold")
    table.add_column("타입")
    table.add_column("태그")
    table.add_column("감정")

    for r in results:
        table.add_row(
            r.id[:8], r.name, r.media_type,
            ", ".join(r.tags[:4]),
            ", ".join(r.emotions[:3]),
        )

    console.print(table)
```

- [ ] **Step 8: 전체 테스트 통과 확인**

```bash
uv run pytest tests/ -v
```

Expected: 전체 PASS (기존 30 + 신규 8 = 38 tests)

- [ ] **Step 9: 커밋**

```bash
git add src/meme_cli/cli.py tests/test_cli.py
git commit -m "feat: semantic search with --semantic flag, embed on add/edit/remove"
```

---

## Self-Review

**스펙 커버리지 체크:**
- [x] 영상클립/GIF/이미지 저장 → Task 6
- [x] AI 자동 메타데이터 생성 → Task 5
- [x] 키워드 검색 (FTS5) → Task 4, 7
- [x] 유사 의미 검색 (벡터) → Task 11-14
- [x] 메타데이터 수정 인터페이스 → Task 8
- [x] AI 친화적 JSON 출력 → `--json` 플래그
- [x] list/get/remove → Task 9

**Placeholder 스캔:** 없음

**타입 일관성:**
- `EmbeddingStore.upsert(item: MediaItem)` — Task 12, 14에서 동일하게 사용
- `EmbeddingStore.delete(item_id: str)` — Task 12, 14에서 동일하게 사용
- `MediaSearch(store, embedding_store=emb_store)` — Task 13, 14에서 동일한 시그니처
- `get_chroma_dir()` — config.py Task 11, cli.py Task 14에서 동일하게 import
